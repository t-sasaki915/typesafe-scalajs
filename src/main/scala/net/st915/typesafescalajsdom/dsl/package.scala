package net.st915.typesafescalajsdom

package object dsl extends attributes.all with syntax.all {

  final type TextNode = net.st915.typesafescalajsdom.TextNode
  final val TextNode = net.st915.typesafescalajsdom.TextNode

  final type Anchor = tags.Anchor
  final val Anchor = tags.Anchor
  final type Area = tags.Area
  final val Area = tags.Area
  final type Audio = tags.Audio
  final val Audio = tags.Audio
  final type BR = tags.BR
  final val BR = tags.BR
  final type Base = tags.Base
  final val Base = tags.Base
  final type Button = tags.Button
  final val Button = tags.Button
  final type Canvas = tags.Canvas
  final val Canvas = tags.Canvas
  final type Dialog = tags.Dialog
  final val Dialog = tags.Dialog
  final type Div = tags.Div
  final val Div = tags.Div
  final type Embed = tags.Embed
  final val Embed = tags.Embed
  final type FieldSet = tags.FieldSet
  final val FieldSet = tags.FieldSet
  final type Form = tags.Form
  final val Form = tags.Form
  final type HR = tags.HR
  final val HR = tags.HR
  final type HTMLMap = tags.HTMLMap
  final val HTMLMap = tags.HTMLMap
  final type IFrame = tags.IFrame
  final val IFrame = tags.IFrame
  final type Image = tags.Image
  final val Image = tags.Image
  final type Input = tags.Input
  final val Input = tags.Input
  final type LI = tags.LI
  final val LI = tags.LI
  final type Label = tags.Label
  final val Label = tags.Label
  final type Legend = tags.Legend
  final val Legend = tags.Legend
  final type Link = tags.Link
  final val Link = tags.Link

}
